# F10 / F13 配合其他机制的利用分析

**主题**:CWSR 寄存器伪造(F10/F13)单独只能作用于**自己**的 wave。本文分析它**配合其他机制**
能达到什么——重点是把"自伤"升级为"跨上下文 wave 状态控制"。排除纯内存破坏,只看逻辑/架构状态层面的组合。
**环境**:MI300X VF(gfx942),ROCm DKMS `amdgpu 6.16.13`,uid 1000,无 CAP_*。
**关联**:`amdgpu-kfd-F10-F13-exploitation.md`(F10/F13 单独利用面)、`amdgpu-kfd-F14-*.md`(F14 跨租户显存原语)。

---

## 0. 结论速览

| 组合 | 能达到什么 | 本次证据 |
|---|---|---|
| **F10/F13 × F14** | 攻击者改写**另一租户**保存区里的 wave 寄存器状态 | ✅ 触及(扫到 3072 块)✅ 写入(落 1024 次 MODE 改写)◑ 影响其执行:竞态/一致性受限,本次未落地 |
| **F10/F13 × 调试 API(同 UID)** | **确定性**伪造另一(同 UID)进程的 wave 状态,无竞态 | ◑ 机制成立(自调试免 ptrace 已确认);跨进程需 ptrace(同 UID) |
| **F10/F13 × 自调试** | 确定性驱动**自己**的 CWSR save/restore(可靠触发故障合成等原语) | ✅ 自调试对 `target==p` 免 ptrace(源码确认) |

**核心洞察**:F10/F13 缺的是"写到别人保存区"的手段;**F14 正好提供跨 UID 的显存写**,
**调试 API 正好提供确定性的 save/restore 驱动**。三者互补。

---

## 1. F10/F13 × F14 —— 跨租户 wave 状态控制

### 思路

- F10/F13:CWSR restore 从**用户可写的保存区**加载 MODE/TRAPSTS/STATUS/PC/EXEC/TTMP,不重新净化。
- 但 F10/F13 单独只能改**自己**进程的保存区。
- F14:非特权用户可把最多 4 GiB 物理显存以读写映射进自己 GPU 页表,已证明能**跨进程读写别的租户的显存**。
- **CWSR 保存区就在显存里**(用户分配的 `ctx_save_restore_area`,dGPU 上通常 VRAM)。

⇒ 用 F14 定位并改写**另一个租户**的 CWSR 保存区 → 该租户的 wave 在下次 restore 时加载攻击者伪造的寄存器。

### 已验证(实机)

**(a) F14 窗口能触及别的租户的 CWSR 块** — 扫描器在 4 GiB 窗口里按特征
(EXEC 对 `0xFFFFFFFF` + `MODE==0x3f0` + STATUS 带 VALID 位)找到 **3072 个** CWSR hwreg 块:

```
+0x069a0b24c  STATUS=0x80010c61 MODE=0x000003f0 PChi~0x0000799c
+0x069a0f14c  STATUS=0x80010c60 MODE=0x000003f0 PChi~0x0000799c
... (3072 blocks)
STATUS=0x80010c61 正是 CWSR 保存侧净化后的值(SPI_PRIO/ECC 已清),确属受害者的保存 wave 状态
```

**(b) F14 能写入别的租户的 CWSR 块** — forge 器把找到的块的 MODE 改成 `0x3f3`(FP_ROUND=3),
单次命中运行落了 **1024 次** MODE 改写到其他进程的保存块里。

### 受限:改写→影响受害者执行(本次未落地)

当 1024 次写入落地时,受害者的确定性计算仍报 `distinct=1`(未被破坏)。原因是**竞态/一致性**:
- 改写只有落在某块的 **save 与 restore 之间**、且该块随后被恢复并执行足够久,才会改变结果;
- 攻击者经 GPU kernel 分批扫写,粒度远粗于自伤版(`cwsr_tamper` 用紧凑 CPU 循环连续拍同一批已知活跃块,才稳定命中);
- 且攻击者经 BAR0 孔径写 VRAM,与 CP 微码读保存区(scalar load)之间可能存在 L2 一致性差异(未深究)。

⇒ **跨 UID 的"写到别人保存区"已证明;把它变成可靠的执行影响受竞态/一致性限制,本次未稳定复现。**
这条链要可靠,更适合叠加调试 API 消除竞态(见 §2),但那需要 ptrace(同 UID)。

### 一旦状态写入生效,可得的跨上下文原语(均为架构状态伪造,非内存破坏)

- **定向单租户 DoS + 甩锅**:伪造受害者 TRAPSTS.MEM_VIOL/ILLEGAL_INST → 受害者队列以"它自己犯了
  内存越界/非法指令"的名义中止(§F10/F13 单独文档已证这些位→对应 HSA 故障类)。可精确打击某个租户,
  并让故障归因到受害者。
- **静默篡改受害者计算**:伪造受害者 MODE(FP_ROUND 等)。
- **劫持受害者 wave 控制流**:伪造受害者保存的 PC/EXEC → 其 wave 在自己 VMID 内跳到攻击者选择的地址执行
  (控制流重定向,非内存破坏)。
- **向调试器伪造异常**:伪造受害者 TRAPSTS 的 EXCP/ADDR_WATCH/HOST_TRAP/XNACK_ERROR → attach 到受害者的
  调试器被喂错误异常。

---

## 2. F10/F13 × 调试 API —— 确定性驱动(消除竞态)

### 思路

CWSR 的触发时机是 §1 竞态的根源。**调试 API 的 SUSPEND_QUEUES 会强制 wave 保存并停在保存态,
直到 RESUME_QUEUES**——这把随机的 save/restore 变成攻击者完全掌握的两个时刻:

```
runtime_enable(ENABLE|TTMP_SAVE)
DBG_TRAP_ENABLE(target)
SUSPEND_QUEUES(target)         → wave 稳定保存在 ctx_save_restore 区
<改写保存区>                    → 无竞态,爱怎么改怎么改
RESUME_QUEUES(target)          → 确定性加载伪造状态
```

### 目标是谁、用什么写

- **`target == self`(自调试)**:`kfd_ioctl_set_debug_trap` 对 `target==p` **跳过 ptrace 检查**(源码确认),
  故进程可无条件自调试。用途:确定性驱动**自己**的 CWSR,可靠触发 F10/F13 的故障合成/调试器伪造等原语
  (不再靠超额订阅碰运气)。
- **`target` = 另一个同 UID 进程**:需 `ptrace_parent(target)==current`,即 ptrace attach(同 UID 即可,无需 CAP)。
  写保存区可直接用 `process_vm_writev`(同 UID 本就能写对方内存),**不需要 F14**。
  ⇒ 同 UID 下:SUSPEND(对方)→ 改对方 CWSR 区 → RESUME,**确定性**伪造对方 wave 状态。
- **`target` = 另一个不同 UID 的租户**:ptrace 跨 UID 被挡 → 无法 SUSPEND 对方。此时只能靠 §1 的 F14 写 +
  自然轮转竞态。

### 意义

- 同 UID 场景(如同一用户下被 seccomp 限制 ptrace 的沙箱之间、或同租户多进程):**确定性**的跨进程 wave
  状态伪造,可注入指定类别故障、篡改计算、重定向 PC——全部是逻辑/架构层伪造。
- 自调试:让 F10/F13 的所有自伤类原语从"概率触发"变成"确定触发",是可靠的构建块。

---

## 3. 组合后仍不可达(诚实定界)

- **无特权提升**:gfx942 上 STATUS.PRIV 伪造不生效(`s_rfe` 清 PRIV,无 gfx11 SW_SA_TRAP 分支,已实测)。
  即使配合 F14/调试 API,也拿不到常驻 PRIV → 拿不到 LDS_ALLOC/s_sendmsg 等特权 hwreg 路径。
  → 这些组合**不产生特权代码执行**。
- **跨 UID 确定性**:被 ptrace 边界挡住;跨 UID 只有 F14 的竞态写,非确定性(本次未落地执行影响)。
- **本质**:全程是架构寄存器状态伪造 + 已知机制的投递,**不涉及内存安全漏洞**(符合"不考虑纯内存破坏")。

---

## 4. 严重性小结

| 攻击者 vs 受害者 | 能力 | 确定性 | 证据 |
|---|---|---|---|
| 同进程(自伤) | 故障合成、MODE 篡改、调试器伪造 | 自调试→确定 | ✅ |
| 同 UID 跨进程 | 上述全部,施加到对方 | 确定(debug+ptrace) | ◑ 机制成立 |
| **跨 UID 跨租户** | 改写对方保存的 wave 寄存器(定向 DoS/甩锅、篡改计算、PC 重定向、调试器伪造) | 竞态(概率) | ✅ 触及+写入;◑ 执行影响受限 |

**要点**:F10/F13 本身是"自伤 + 调试器伪造"级别;**配合 F14**,它获得了触及并改写**其他租户**保存 wave
状态的能力(触及与写入已实机证明),从而具备定向单租户攻击的形态;**配合调试 API**,在同 UID 范围内变为
确定性。真正的跨租户内存读写仍由 F14 独立提供;本组合新增的是"跨租户**架构状态/控制流**操纵"这一维度。

---

## 5. 修复

- 堵 F10/F13 源头(恢复路径重新净化,见主文档)即可消除"伪造保存区→加载进硬件"这一投递,
  从而同时废掉本文所有组合。
- 堵 F14 源头(USERPTR→DOORBELL 尺寸校验)则切断跨 UID 的写达手段。
- 两者独立修复,任一到位都显著削弱组合链。

---

## 附:PoC

位于 scratchpad:
- `f14_cwsr_scan.hip` — F14 窗口扫描别租户 CWSR 块(证明触及,3072 命中)
- `f14_cwsr_forge.hip` / `f14_cwsr_forge2.hip` — 经 F14 改写别租户 CWSR MODE(证明写入,1024 命中;连续版尝试打竞态)
- `det_victim.hip` — 确定性计算受害者(报告 distinct,基线=1)
- `cwsr_forge.hip` / `rdprobe.hip` — F10/F13 单独伪造+回读框架(见单独文档)

测试全程无 GPU reset、无内核 oops;清理后 VRAM 回 0.28 GB,GPU 正常。
